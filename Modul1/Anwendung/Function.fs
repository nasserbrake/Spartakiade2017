module Function

// Mit Zwischenergebnis
// Add
let add x y = 
    let z = x + y
    z

let isEven x = 
    let z = x % 2 = 0
    z

let length (x:string) = 
    x.Length

let add1 x = 
    let z = x + 1
    z

let addOneToInt x = x + 1
let addOneToFloat x = x + 1.0
let addMoreToString x = x + "More"